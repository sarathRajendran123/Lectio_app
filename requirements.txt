# ── Web Framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==13.1

# ── Pydantic & Settings ────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# ── Database ───────────────────────────────────────────────
# (alembic + psycopg2-binary removed -- migrations run as raw SQL via
#  docker-entrypoint-initdb.d and all DB access goes through asyncpg)
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0

# ── Auth ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
cryptography==43.0.1

# ── LLM & Agent Framework ──────────────────────────────────
langchain==0.3.3
langchain-core==0.3.10
langchain-community==0.3.2
langchain-groq==0.2.0          # Free LLM via Groq
langgraph==0.2.28
langsmith==0.1.131

# ── Document Parsing ───────────────────────────────────────
pymupdf==1.24.11               # PDF parsing (imported as `fitz`)
python-docx==1.1.2             # DOCX parsing
python-pptx==1.0.2             # PPTX parsing
pytesseract==0.3.13            # OCR

# ── Embeddings & RAG ───────────────────────────────────────
sentence-transformers==3.1.1   # BGE embeddings (local, free)
chromadb==0.5.12               # Vector DB
rank-bm25==0.2.2               # BM25 sparse retrieval

# ── Utilities ──────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.2
aiofiles==24.1.0
pillow==10.4.0
numpy==1.26.4
tenacity==8.5.0                
python-json-logger==2.0.7      
